https://en.wikipedia.org/wiki/Linux_namespaces#History
---

<pre>
  PID (namespace):
  From the host machine you can see the container process id, but from container you can see only container process IDs.
  Network (namespace):
  It is isolated for both, host to cotainer, and container to host.
</pre>
