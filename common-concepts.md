1. When you delete container (/var/lib/docker/container/), volume and image persit.
2. When you delete image(from folder /var/lib/docker/overlay2/) volume ( /var/lib/docker/volumes/) persist.
3. when you delete volume, image does not persist.
